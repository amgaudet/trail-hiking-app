<div class="section">
  <div class="container">
    <div class="columns is-multiline is-centered">
      {{#each locations.trails as |trail|}}
      {{> search-details}}
      {{/each}}
    </div>
  </div>
</div>

<p>Hello</p>
